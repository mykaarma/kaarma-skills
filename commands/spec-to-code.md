---
description: "Spec-driven development — parses a spec into missions, executes with test validation, loops until all acceptance criteria pass"
disable-model-invocation: true
---

Invoke the spec-to-code skill and follow it exactly.
